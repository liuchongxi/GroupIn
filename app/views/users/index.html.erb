<p id="notice"><%= notice %></p>

<h1>Listing Users</h1>

<table>
  <thead>
    <tr>
      <th>First name</th>
      <th>Last name</th>
      <th>Email</th>
      <th>Password</th>
      <th>Gender</th>
      <th>Major</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.first_name %></td>
        <td><%= user.last_name %></td>
        <td><%= user.email %></td>
        <td><%= user.password %></td>
        <td><%= user.gender %></td>
        <td><%= user.major %></td>
        <td><%= link_to 'Show', user,{:style=>'color:blue;', :class => "css_class"} %></td>
        <td><%= link_to 'Edit', edit_user_path(user), {:style=>'color:blue;', :class => "css_class"}  %></td>
        <% if logged_in?%>
        <% if loggedin_user.admin? %>
        <td><%= link_to 'Destroy', user, method: :delete, :style=>'color:blue;', :class => "css_class", data: { confirm: 'Are you sure?' } %></td>
        <% end %>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New User', new_user_path, {:style=>'color:blue;', :class => "css_class"}%>
