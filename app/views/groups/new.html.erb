<% if logged_in? %>
<h1>New Group</h1>
<%= render 'form' %>

<%= link_to 'Back', groups_path,  {:style=>'color:blue;', :class => "css_class"} %>
<% else %>
<p style="color:red;font-size:200%;"> Please log in before creating  group</p>
<%end%>
